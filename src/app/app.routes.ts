import {Routes} from '@angular/router';
import {UsersTableComponent} from 'src/components/users-table/users-table.component';

export const routes: Routes = [
    {
        path: '',
        component: UsersTableComponent,
    },
];
